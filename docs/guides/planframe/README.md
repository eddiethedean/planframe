# PlanFrame guides

This section is about the **core** `planframe` package.

- **Creating an adapter**: see `creating-an-adapter.md` (includes the `group_by_agg` contract: compiled group keys and mixed tuple / expression aggregations).

For **end-to-end Polars examples** (including `group_by` / `agg`), see `../planframe-polars/using-planframe-polars.md`.
